# OS
.DS_Store

# Editor / build artifacts
*.tsbuildinfo

# Captured network logs (may contain auth tokens — never commit)
*.har

# Local design references / scratch
stitch_ai_creator_feed/

docs/

# Heavy raw mascot source GIFs — ship optimized webp in public/mascot/ instead
frontend/crow_*.gif
